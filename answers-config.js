// Public configuration only. This file must never contain a password, GitHub token, or parent code.
// It stays empty until the separately deployed answer service is verified.
window.ENGLISH_ANSWER_API = '';
